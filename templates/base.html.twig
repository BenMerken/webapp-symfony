<!DOCTYPE html>
<html lang="nl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>{% block title %}Welcome!{% endblock %}</title>
    {% block stylesheets %}
        <link rel="stylesheet" href="{{ asset('css/base.css') }}">
    {% endblock %}
</head>
<body>
{% block body %}
    <div class="background">
        <div class="background-overlay">
            <nav class="navbar">
                <a href="https://www.pxl.be"><img src="{{ asset('images/pxl-logo.png') }}" alt="PXL logo"></a>
                <a class="home-button" href="{{ url('home') }}">Home</a>
                {% if app.user %}
                    {% if is_granted('ROLE_ADMIN') %}
                        <a class="login-button" href="{{ url('admin_dashboard') }}">Admin dashboard</a>
                    {% elseif is_granted('ROLE_MOD') %}
                        <a class="login-button" href="{{ url('moderator_dashboard') }}">Mod dashboard</a>
                    {% elseif is_granted('ROLE_CUSTODIAN') %}
                        <a class="login-button" href="{{ url('custodian_dashboard') }}">Custodian dashboard</a>
                    {% endif %}
                    <a href="{{ url('logout') }}">Logout</a>
                {% else %}
                    <a class="login-button" href="{{ url('login') }}">Login</a>
                {% endif %}
            </nav>
            <div class="content">{% block content %}{% endblock %}</div>
            <footer class="credits">
                {% block footer %}
                    Created by Peter Janssen & Ben Merken @ Hogeschool PXL, 2019.
                {% endblock %}
            </footer>
        </div>
    </div>
{% endblock %}
</body>
</html>
